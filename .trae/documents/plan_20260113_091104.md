## 优化计划：店铺选择和信息显示

### 1. 优化店铺选择功能

**修改文件：** `src/views/login.vue`

**具体改动：**

* 更新店铺选择对话框的HTML结构，使每个店铺条目成为可点击的整体

* 添加点击事件处理，实现点击勾选框或店铺名称都能选择店铺

* 确保选择状态正确更新和存储

### 2. 在导航栏显示当前店铺和商家信息

**修改文件：** `src/layout/components/Navbar.vue`

**具体改动：**

* 在导航栏右侧添加信息显示区域

* 从localStorage获取当前店铺名称并显示

* 添加商家信息获取逻辑，调用 `/mch/merchant/mine` 接口获取 `loginAccount`

* 处理未选择店铺的情况（显示默认提示）

* 添加适当的样式和图标，确保视觉一致性

### 3. 添加商家信息API调用

**修改文件：** `src/api/mall/merchant.js`

**具体改动：**

* 添加 `getMerchantInfo` 函数，调用 `/mch/merchant/mine` 接口

* 确保API调用正确处理响应和错误

### 实现步骤：

1. 首先修改登录页面的店铺选择逻辑，确保点击店铺名称也能选择
2. 添加商家信息API调用函数
3. 修改导航栏组件，添加店铺和商家信息显示
4. 测试所有场景，确保功能正常运行

